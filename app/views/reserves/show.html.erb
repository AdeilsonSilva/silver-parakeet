<p id="notice"><%= notice %></p>

<p>
  <strong>Id:</strong>
  <%= @reserve.id %>
</p>

<p>
  <strong>Initials:</strong>
  <%= @reserve.initials %>
</p>

<p>
  <strong>Description:</strong>
  <%= @reserve.description %>
</p>

<%= link_to "Empréstimos", reserf_loans_path(@reserve) %>
<%= link_to "Reservas de Material", reserf_material_reserves_path(@reserve) %>

<%= link_to 'Edit', edit_reserf_path(@reserve) %> |
<%= link_to 'Back', reserves_path %>
