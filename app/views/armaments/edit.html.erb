<% content_for :title, "Usuários" %>

<div class="off-xs-3 xs-6 text-center">
    <div class="padding">
        <h1>Edição de Armamento</h1>

        <%= render 'form', armament: @armament %>
    </div>
</div>
