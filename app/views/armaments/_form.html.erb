<%= form_with(model: armament, local: true) do |form| %>
  <% if armament.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(armament.errors.count, "error") %> prohibited this armament from being saved:</h2>

      <ul>
      <% armament.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :serial_number %>
    <%= form.number_field :serial_number, id: :armament_serial_number %>
  </div>

  <div class="field">
    <%= form.label :manufacturer %>
    <%= form.text_field :manufacturer, id: :armament_manufacturer %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
